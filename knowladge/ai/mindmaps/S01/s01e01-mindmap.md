# S01E01 — Programming Interactions with Language Models

## 1. Controlling Model Behavior via Code

- **LLMs as application logic** — process text, image, audio, video via API
- **Deterministic code + non-deterministic outputs** — core tension of generative apps
- **Autoregressive generation**
  - Token-by-token prediction
  - Each new token influenced by ALL preceding tokens (input + generated so far)
  - Generated tokens **cannot be deleted** mid-generation
  - Implication: system prompt, user message, AND assistant's own output all steer the result
  - **Visualization**: API request with `model: "gpt-5.2"`, `stream: true`, `max_output_tokens: 16` — 5 sequential steps show context window growing with each token: "Hello." → "Hello" → "-" → "how" → "can" → "I" — each step the model sees original input + all previously generated tokens
- **Stateless API**
  - Every request sends full context (no server-side memory)
  - Developer controls what goes into each request
  - Context window = hard limit (input + output tokens combined)
- **Tokens**
  - Content split into sub-word fragments (tokenizer-dependent)
  - Language matters: Polish ~50-70% more tokens than English for same content
  - Tool: [Tiktokenizer](https://tiktokenizer.vercel.app/) for inspection
  - **Tokenized sequence anatomy**: messages wrapped with special tokens (`<|im_start|>`, `<|im_end|>`) marking message boundaries — system/user/assistant roles + content all converted to numeric token IDs (e.g., 288266, 17368, 3576...) — three token categories: special tokens, role, content
- **Multi-step interactions**
  - Chain requests: classify first, then act based on result
  - User sees simple Q&A; behind the scenes = routing + specialized prompts
  - **Routing example**: Request 1 classifies intent ("refund") with specialized system prompt → classification result dynamically adjusts Request 2's system prompt ("You are a **refund** specialist. Be empathetic. Explain the process. Ask for order number.") → model behavior changes based on routing decision without needing separate specialized models
  - **Key lesson: steering = managing context across multiple chained requests**
- **Code interaction pattern**: conversation array grows with each turn — previous responses appended as assistant messages before next request — full history sent each time for multi-turn coherence

## 2. Structured Outputs & JSON Schema

- **Two API mechanisms**: Structured Outputs vs Function Calling
- **Structured Outputs** — transform unstructured data into any shape:
  - Inference, extraction, compression, enrichment, paraphrase
  - Translation, classification, verification, generation, synthesis
- **Structured Output flow**: unstructured input (e.g., "Meet Anna tomorrow 3pm, Q1 planning") + JSON Schema → guaranteed structured JSON output → `JSON.parse()` → type-safe property access → no try/catch needed, eliminates regex/fallback parsing logic
- **JSON Schema has two dimensions**:
  - **Structure** — guaranteed in `strict` mode (shape is certain)
  - **Values** — generated by model based on content + field names + descriptions
    - Names and descriptions ARE instructions for the model
    - LLM can help draft schemas, but human must validate quality
- **Critical design patterns**:
  - **Include unknown/neutral values** — "unknown", "neutral", "mixed" prevent forced hallucinations
  - **Field order matters** — `reasoning` first drives `sentiment`, which drives `confidence`
    - Autoregressive: earlier fields steer later fields
  - Descriptions must be precise AND concise
  - **Schema design example**: sentiment classification with `reasoning` (type: string, "Quote 1-2 words from input that justify the classification"), `sentiment` (enum: positive/negative/neutral/mixed/unknown, "Use 'mixed' for conflicting emotions"), `confidence` (enum: high/medium/low, "0.80: requires explicit emotional word") — descriptions act as inline prompts for each field
- Example: [01_01_structured](https://github.com/i-am-alice/4th-devs/tree/main/01_01_structured)

## 3. Rendering LLM Responses (LLM & LRM)

- **Response is NOT just text** — it's a series of events:
  - Reasoning tokens (sometimes only summary)
  - Native tool signals
  - Function calling invocations
  - Generated images, target text, errors
- **Response anatomy**: user query triggers a streaming event sequence — (1) reasoning section with thinking tokens and budget, (2) API search invocations with results, (3) tool calls with parameters and "APPROVED" badge, (4) structured results returned, (5) final text output — all integrated sequentially in response events
- **Streaming** — output can be streamed, paused for user confirmation, or awaiting parallel agents
- **Rendering challenges**:
  - Markdown → HTML conversion during streaming
  - LaTeX, code blocks
  - Libraries: [Streamdown](https://streamdown.ai/), [Markdown Parser](https://github.com/Simon-He95/markstream-vue/tree/main/packages/markdown-parser)
- **Interactive components** — AI-generated UIs:
  - **UI Mode**: user asks "What's the current BTC price?" → model returns JSON with `type: "Card"`, `title`, `price`, `change`, `series` → frontend renders card component with price display, change indicator, 7-day line chart, min/avg/max data cards
  - Rendering becomes an app problem: validate JSON, map to components, handle errors and partial streaming, keep UI responsive
  - [MCP Apps](https://blog.modelcontextprotocol.io/posts/2025-11-21-mcp-apps/)
  - [Apps SDK](https://developers.openai.com/apps-sdk)
  - [JSON Render](https://github.com/vercel-labs/json-render)
- **Key takeaway: agent interactions are ALWAYS event-based** — especially for multi-agent background systems

## 4. UI vs Application Logic — The Three Layers

- **Layer 1: LLM output** — generates (usually) text, structured or unstructured
- **Layer 2: Application** — dynamic UI, event reactions, state management, context management
  - Controls what portion of generated content reaches the user
  - Manages conversation context (may send only partial data to model)
- **Layer 3: User view** — representation of current app state, far from raw LLM output
- **Anti-pattern: storing raw message text**
  - Simple at first, becomes severe limitation in production
  - Any expansion requires changes across DB → backend → frontend + data migration
  - **"Raw text chunks only"**: backend emits only `text_delta` events → no context for frontend → reasoning tokens leak into UI → no way to show "Thinking..." indicator, collapse internal thoughts, or style different phases → confusing and unprofessional UX
- **Solution: Semantic Events**
  - Events carry text + metadata (ID, type, properties)
  - Enable grouping, enrichment, UI-specific properties
  - Easy to extend (just add new properties)
  - **Streaming timeline**: reasoning phase emits `thinking_start` / `thinking_delta` / `thinking_end` events (with `id` for grouping) → output phase emits `text` events → frontend renders "Thinking..." indicator (collapsible), shows duration ("Thought for 2.3s"), then displays final text — `id` field connects related events, `type` field tells frontend what phase is streaming
  - **Tool-calling events**: `tool_call` event with id, name, args → frontend shows function card with "Approve" button → stream pauses → user approval is frontend state change (no backend event) → `tool_result` streams back → final `text` output — enables pause-and-resume flows with user confirmation
  - Same pattern works for tool-calling events
  - Enables building rich interactive interfaces from structured event streams

## 5. Model Selection Strategies

- **No "best model"** — only "best model for THIS situation"
- **Stay current**: follow AI companies + researchers on X/LinkedIn
  - Key companies: OpenAI, Anthropic, DeepMind, xAI, DeepSeek, z.ai, Qwen, ByteDance, Kimi, BFL, Kling, HuggingFace
  - Track: OpenRouter, Replicate for new model announcements
- **Evaluation process**:
  - Hard challenges from your domain
  - Tasks other models fail at
  - Existing test suites
  - Unique puzzles testing relevant abilities
  - "Vibe check"
  - Switch production apps to new model and observe
  - Automated: [Promptfoo](https://www.promptfoo.dev/), [DeepEval](https://deepeval.com/)
- **Four model strategies**:
  1. **Single main model** — simplest, one model for everything
  2. **Main + Alternative** — most common; big model for hard tasks, small for the rest (cost/speed optimization)
  3. **Main + Specialists** — prioritize effectiveness per domain (z.ai for components, Anthropic/Kimi for text, xAI for file exploration)
  4. **Team of small models** — decomposition + voting; high quality without expensive models; rare, demanding approach
- **Avoid vendor lock-in** — multiple models = multiple providers; leaders change fast

## 6. Key API Capabilities Across Providers

- Providers: OpenAI, Anthropic, Gemini (DeepMind), xAI, OpenRouter
- **Multimodality** — image gen/edit, audio, video
  - Best: Gemini Interactions API, then OpenAI Responses API
- **Model availability** — text, audio, image, video, embeddings, real-time
  - Best: Gemini + OpenAI
- **Native tools** — web search, deep research, file search, computer use, code execution
  - Limited configuration; simple to use but hard to customize
- **Cache** — critical for agents at scale
  - Reduces re-processing of input tokens → lower cost + faster response
- **Rate Limits** — quickly become a challenge post-development
  - Request increases early; consider OpenRouter as alternative
- **Frameworks NOT recommended** — too many constraints, not enough value
  - Use official SDKs directly (AI SDK mention but not endorsement)
  - AI_devs teaches the underlying mechanics

## 7. Organizing Instructions in Code

- **Four approaches**:
  1. **Inline** — directly at call site; good for simple, stable prompts
  - Hardcoded in API calls, least flexible
  2. **Separate files** — prompt composition with dynamic sections + variables; most popular
     - Built with functions enabling parameterization (e.g., `buildPrompt(role, context)` with template literals)
     - Requires monitoring systems to observe final prompt content
  3. **External systems** — e.g., [Langfuse](https://langfuse.com/); versioning + monitoring outside codebase
  - Named prompts with version dropdown + deploy button — enables A/B testing
  4. **Markdown files with YAML frontmatter** — accessible at runtime from filesystem
     - YAML frontmatter for config (model, temperature) + Markdown body with `{{variables}}` template syntax — source-control friendly
     - **Becoming the #1 choice for multi-agent systems**
     - Agents can dynamically read, generate, and edit prompts
- **Best single choice: Markdown files** — combines benefits of all others
  - Agents can create new skills for other agents (Architect → Reporter pattern)
  - Same approach as Claude Code, Cursor
- **Dynamic skill generation**:
  - **Architect agent** (`architects/architect.md`, claude-4-omni) creates skill file in `/skills` → updates target agent's skills field → spawns agent with new capability
  - **Reporter agent** (`agents/reporter.md`) executes using injected skill (e.g., `skills/linear-fetcher.md` with `allowed-tools: list.issues, get.issue`) — generates daily reports
  - Enables **runtime agent augmentation without code changes** — zero-cost skill injection
  - Possible thanks to models' deep knowledge of prompt design
  - Caution: [SkillsBench](https://arxiv.org/abs/2602.12670) suggests careful validation

## 8. Prompt Engineering — Still Essential for Builders

- Users of agents (Cursor, Claude Code) don't need it much
- **Builders of agents absolutely need it**
- **System prompt anatomy** (from Claude's published prompts):
  - **Structuring** — XML-like tags (`<context>`, `<rules>`, `<instructions>`) marking section boundaries
  - **Identity** — general context + scope (role steers associations, not magic skill boost)
  - **Limits** — reduce risk of unwanted actions (no guarantee of compliance)
  - **Constraints** — conditional action rules (e.g., find item before editing it)
  - **Style** — tone, format, structure; affects UX + system speed
  - **Security** — against malicious users; can be bypassed but still reduces risk
  - **Adaptation** — handling situations model can't natively address (missing info, inactive features)
  - **Calibration** — specific formatting instructions (e.g., Mermaid diagrams), confidence thresholds, escalation rules
- **Prompt = a sandbox** — space where model can operate within defined rules
  - **Cannot fully prevent** malicious instructions or unexpected behavior
  - Must address at code level + design assumptions
- **Debugging prompts** — unlike code, you never know exactly WHY a behavior occurs
  - Add rules that increase probability of desired effects
  - Observe model behavior as your feedback loop
- **"Generalizing the generalization"**
  - **Level 1 (Problem)**: tools `add_task` and `add_event` — user says "Add meeting with Tom tomorrow 5pm" → model picks wrong tool
  - **Level 2 (Bad fix)**: specific patch "If user mentions 'meeting', use add_event" → creates cascading edge cases ("Remind me to prepare for the meeting" → wrong again) — **whack-a-mole**: each patch creates new edge cases
  - **Level 3 (Good fix)**: meta-rule: "Before selecting any tool: 1. State which tool and why, 2. Rate certainty (0-100%), 3. If <30%, ask one clarifying question" → transfers to ALL ambiguous cases across any tool configuration
  - Rules about **how to decide** are more robust than rules about **which choice**

## 9. Specializing Models — Context, Few-shot, Many-shot

- **In-context learning** — models detect patterns in the prompt itself
- **External knowledge** — just include it in context; model will use it
- **Teaching new skills** — describe rules + show examples (few-shot)
  - Model recognizes patterns in examples and applies them
  - **Few-shot example**: `<examples>` section in system prompt with email classification — input-output pairs showing raw email metadata → classification labels (INBOX, SPAM, SUPPORT) with urgency, sender type, content category tags — model learns classification patterns from concrete examples, reducing hallucination and improving consistency
- **Evolution of context loading**:
  - **Old (static injection)**: hardcoded `<context>` with user data (name, location, timezone) loaded regardless of query relevance — bloated prompts + stale data + no flexibility
  - **New (dynamic loading)**: equip model with tools (`get_user_profile`, `get_weather(location)`) to load external documents on demand — context loaded only when needed, always fresh
- **System prompt guidelines**:
  - Keep it generalized — essential rules for interaction, tool use, context exploration
  - Avoid noise — don't include content that's irrelevant most of the time
  - Specific knowledge loaded dynamically when needed
- Models use context well, but problems remain from insufficient knowledge or poor presentation

## 10. Structured Outputs in Practice — Grounding Tool

- **Goal**: process markdown files → extract keywords → web-search definitions → generate HTML with tooltips
- **Grounding concept**: text with highlighted keywords — hovering shows tooltip with verified definition + source URLs (arxiv.org, research.google, papers.nips.cc) — links abstract terminology to verified definitions with traceability
- **Pipeline**:
  1. **Extract keywords** — split text into paragraphs, process chunks (not full doc)
  - JSON Schema `concept_extraction`: each concept has `id` (canonical name), `category` (enum), `searchQuery` (nullable — skip if no verification needed), `reason` (why extracted) — AI-generated schema with precise field names
  2. **Merge duplicates** — LLM identifies aliases across different spellings
  - Groups synonyms under canonical labels: `{ canonical: "GPT-4", aliases: ["gpt4", "GPT 4"] }`
  3. **Web search** — use OpenAI native tools to find definitions
  - Returns summaries with source URLs for external validation
  4. **Generate HTML** — final document with template + tooltips
  - Injects grounded spans: `<span class="grounded" data-grounding='{...}'>GPT-4</span>`
- **Project structure**: `app.js` (entry point), `template.html` (tooltip CSS/JS), `src/` with `api.js` (OpenAI client), `schemas/` (JSON Schema definitions), `prompts/` (system instructions per stage), `pipeline/` (stage logic), `notes/` (input), `output/` (generated files)
- **Key engineering patterns**:
  - **Chunked processing** — even if text fits context window; improves focus → higher accuracy → enables smaller/cheaper models
  - **Parallel + batched requests** — reduces execution time while avoiding rate limits
  - **File-based checkpointing** — save progress as JSON; resume on failure
  - **Cache** — skip regeneration if files already exist; intermediate cache layers (dedupe, search results) reused across runs; explicit `--force` flag to override — terminal output shows which steps were cached vs. executed
- Example: [01_01_grounding](https://github.com/i-am-alice/4th-devs/tree/main/01_01_grounding)

## 11. Database Structures for Chatbots & Agents

- **Simple chatbot**: 2 tables, 10 fields total
  - **conversations**: id (uuid), user_id (uuid), title (varchar), created_at, updated_at
  - **messages**: id (uuid), conversation_id (uuid), role (enum: user/assistant), content (text), created_at
  - Relationship: conversation 1:n messages
- **Multi-agent system**: 3 tables
  - **sessions** — id, user_id, status, title, created_at, updated_at — orchestration context
  - **agents** — id, session_id, parent_id (delegation chain), template, role, config (json), workspace_path (isolation), status, writing_to_call_id (bidirectional communication), created_at, started_at, completed_at (lifecycle tracking)
  - **items** — id, agent_id, type, role (enum), call_id, arguments (json), thought_structure (json), created_at — stores execution context
  - Relationships: session 1:n agents, agent 1:n agents (parent-child hierarchy), agent 1:n items
- **Enables**: team collaboration, bidirectional communication, user approval flows
- **Example flow**: User requests "Send me a daily report from Claire to my email" → **Alice** (coordinator) calls `spawn_agent` to delegate to Claire (status: waiting, writing_to_call_id=call_465) → **Claire** (reporter) calls `list_issues` on Linear API → returns summary (3 completed, 3 in progress, 2 blockers, key AUTH-142 deployed, API-99 needs review) → status: completed → Alice receives result → calls `send_email` → confirms to user — each agent maintains independent items while coordinating via parent session

## 12. Open-Source Models & Local Inference

- **Tools**: llama.cpp, MLX, [LM Studio](https://lmstudio.ai/)
- **Reality**: most companies stay with commercial LLMs (API or Bedrock/Azure)
- **When useful**: specialized tasks, privacy requirements
- **Key concepts**:
  - **Formats**: GGUF (cross-platform via llama.cpp), MLX (Apple Silicon)
  - **Parameters**: 7B, 70B etc. — more = better quality but higher requirements
  - **Quantization**: Q8 → Q4 → Q2 = more compression, less precision; Q4/Q5 = sweet spot
  - **Context window**: same as commercial but larger window = rapidly growing memory usage
  - **Hardware**: VRAM (GPU) or Unified Memory (Apple Silicon); 32GB+ minimum, 512GB for largest models
- **Recommended test models**:
  - Nemotron 3 Nano, GLM 4.7 Flash, Qwen 3 Coder A3B
- **Alternative**: test via OpenRouter (GLM 4.7, Minimax 2.1)

## 13. Knowledge Sources & Community

- **Key people**: Karpathy, Hassabis, Amodei, Kilpatrick, Sanseviero, Goldberg, Schmid, Albert, Dodds, Delangue, Teknium, Gerganov, Liu, Fioravanti, Hannun, Canuma, elder_plinius, Shapiro
- **YouTube**: AI Explained, AI Engineer, Discover AI, Stanford Online
- **Platforms**: HuggingFace, Google AI Developers, Claude Code Community, OpenRouter, Replicate, Unsloth
- **Tools**: Cursor, AI SDK, LM Studio, Promptfoo, Langfuse
- **Companies**: Stability AI, Kling AI
